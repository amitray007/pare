"""Quality presets for benchmark runs.

Re-exports from constants for backwards compatibility.
"""

from benchmarks.constants import (  # noqa: F401
    ALL_PRESETS,
    HIGH,
    LOW,
    MEDIUM,
    PRESETS_BY_NAME,
    QualityPreset,
)
